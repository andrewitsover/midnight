{
  "name": "@andrewitsover/midnight",
  "version": "4.0.7",
  "description": "An ORM for SQLite",
  "types": "index.d.ts",
  "type": "module",
  "exports": {
    "import": "./index.js",
    "require": "./index.cjs"
  },
  "scripts": {
    "build": "./node_modules/.bin/esbuild index.js --bundle --platform=node --format=cjs --packages=external --outfile=index.cjs"
  },
  "author": "Andrew Jones",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^25.6.2",
    "esbuild": "^0.28.0"
  },
  "engines": {
    "node": ">=26.1.0"
  }
}
